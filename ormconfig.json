{
   "type": "mysql",
   "host": "localhost",
   "port": 3306,
   "username": "root",
   "password": "",
   "database": "secret",
   "synchronize": true,
   "logging": false,
   "entities": [
      "dist/**/*.entity.{js,ts}"
   ],
   "migrations": [
      "dist/src/database/migrations/*.{js,ts}"
   ],
   "cli": {
      "migrationsDir": "src/database/migrations"
   }
}